{
  "rules": {
    ".read": false,
    ".write": false,

    "fa-users": {
      ".read": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')",
      "$userKey": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "fa-ranking": {
      ".read": true,
      "$userKey": {
        ".write": "auth != null"
      }
    },

    "fa-progress": {
      ".read": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')",
      "$userKey": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "fa-admins": {
      ".read": "auth != null",
      "$userKey": {
        ".write": "auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br'"
      }
    },

    "turmas-interesse": {
      ".read": "auth != null",
      "$turmaKey": {
        "$userKey": {
          ".write": "auth != null"
        }
      }
    },

    "turmas-interesse-log": {
      ".read": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')",
      "$turmaKey": {
        "$userKey": {
          ".write": "auth != null"
        }
      }
    },

    "turmas-config": {
      ".read": "auth != null",
      "$turmaKey": {
        ".write": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')"
      }
    },

    "turmas-checkin": {
      ".read": "auth != null",
      "$turmaKey": {
        "$data": {
          "$userKey": {
            ".write": "auth != null"
          }
        }
      }
    },

    "fa-seeds-hidden": {
      ".read": "auth != null",
      "$key": {
        ".write": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')"
      }
    },

    "fa-seeds-deleted": {
      ".read": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')",
      "$key": {
        ".write": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')"
      }
    },

    "fa-holocron-hidden": {
      ".read": "auth != null",
      "$key": {
        ".write": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')"
      }
    },

    "fa-reset-signal": {
      "$userKey": {
        ".read": "auth != null",
        ".write": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br')"
      }
    },

    "players": {
      ".read": true,
      "$key": {
        ".write": "auth != null"
      }
    },

    "holocron": {
      ".read": true,
      "$itemId": {
        ".write": "auth != null"
      }
    }
  }
}
