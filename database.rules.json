{
  "rules": {
    ".read": false,
    ".write": false,

    "fa-users": {
      "$userKey": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "fa-ranking": {
      ".read": true,
      "$userKey": {
        ".write": "auth != null"
      }
    },

    "fa-progress": {
      "$userKey": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "fa-holocron": {
      ".read": true,
      "$itemId": {
        ".write": "auth != null"
      }
    },

    "fa-admins": {
      ".read": "auth != null",
      "$userKey": {
        ".write": "auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br'"
      }
    },

    "turmas-interesse": {
      "$turmaKey": {
        ".read": "auth != null",
        "$userKey": {
          ".write": "auth != null"
        }
      }
    },

    "turmas-config": {
      ".read": "auth != null",
      "$turmaKey": {
        ".write": "root.child('fa-admins').child(auth.uid).exists()"
      }
    },

    "turmas-checkin": {
      "$turmaKey": {
        ".read": "auth != null",
        "$data": {
          "$userKey": {
            ".write": "auth != null"
          }
        }
      }
    },

    "fa-seeds-hidden": {
      ".read": "auth != null",
      "$key": {
        ".write": "auth != null && (auth.token.email === 'tatianefdirene@previ.com.br' || auth.token.email === 'danielfrazao@previ.com.br' || root.child('fa-admins').child(auth.uid).exists())"
      }
    },

    "players": {
      ".read": true,
      "$key": {
        ".write": "auth != null"
      }
    },

    "holocron": {
      ".read": true,
      "$itemId": {
        ".write": "auth != null"
      }
    }
  }
}
